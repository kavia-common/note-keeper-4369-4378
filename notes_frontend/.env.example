# Public URL of the backend API that serves notes endpoints.
# Example: http://localhost:8000
NEXT_PUBLIC_BACKEND_URL=
# Optional: Public site URL for sitemap generation (defaults to http://localhost:3000 if not set)
NEXT_PUBLIC_SITE_URL=
